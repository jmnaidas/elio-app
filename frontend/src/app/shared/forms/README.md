# Form conventions

Basic native control styling lives in global SCSS. Future controls must preserve labels, keyboard behavior, validation messages and accessible descriptions; introduce components only when a real feature needs them.

